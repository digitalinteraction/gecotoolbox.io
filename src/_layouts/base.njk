<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{{ title + ' |' if title }} {{ site.title }}</title>
    <link rel="stylesheet" href="{{ '/styles.css' | url }}" />
    <link rel="icon" href="{{ '/static/favicon.png' | url }}" />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css?family=Rubik:500,400|IBM+Plex+Sans:400,600,400i&display=swap"
    />
  </head>
  <body>
    <div class="fill-viewport-vertically">
      {{ content | safe }}
    </div>
  </body>
</html>
